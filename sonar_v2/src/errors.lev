namespace Sonar {
    interface ISonarException : IException { }

    class SonarException : Exception, ISonarException {
        new SonarException(string message) { this.message = message; }
    }
}
