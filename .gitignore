build/
*.o
*.a
compile_commands.json
.cache/
Testing/

# LA-2: per-triple runtime link-flag sidecars are generated by build-triple.sh
runtime/*/lvrt.link
lvrt.link
build-asan/

# Stray trident-build output when run from repo root without --out
/helm
/recon
/sandbox/
